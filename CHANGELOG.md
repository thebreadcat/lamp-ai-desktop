# Changelog

## 0.2.1 — 2026-05-26

- Bundle latest Lamp from `thebreadcat/lamp-ai` (Workshop submodule included in CI).
- Fix packaged app icons (tray, dock, setup window) on macOS and other platforms.
- Add runtime icon resources for packaged builds.
- Fix Linux `.deb` packaging metadata (author email).
- Fail CI icon generation when `iconutil` fails on macOS.

## 0.2.0

- First public test release: one-click setup wizard, Ollama install flow, multi-platform CI builds.
